{
  "name": "@fixtures/ssr",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "start": "JOE_CLIENT=1 node --no-warnings --loader tsx --loader joe-dom/node-loader --conditions source src/server.tsx"
  },
  "dependencies": {
    "joe-dom": "*",
    "mime-types": "2.1.35",
    "tsx": "3.12.7",
    "typescript": "5.1.3"
  },
  "devDependencies": {
    "concurrently": "8.2.0",
    "esbuild": "0.18.4"
  }
}
